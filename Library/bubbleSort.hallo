note bubbleSort nums arr:
   num size_arr.
   size_arr is size of arr.
   num i,j.
   nums number.
   repeat i from 0 to size_arr - 1 update inc by 1:
      repeat j from 0 to size_arr - i -1 update inc by 1:
         if arr j greater than number j+1 then:
            num temp.
            temp is arr j.
            arr j is arr j+1.
            arr j+1 is temp.
            done.
         done.
      done.
   send arr.